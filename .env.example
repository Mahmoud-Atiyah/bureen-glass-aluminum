# Public site origin (no trailing slash). Used for canonical/OG/Twitter URLs,
# sitemap, robots, and JSON-LD. Set this in Vercel for each environment.
NEXT_PUBLIC_SITE_URL=https://bureenglass.com

# --- Quote form email delivery (FormSubmit.co, no API key) ---
# Inbox that receives quote requests. The quote form POSTs server-side to
# FormSubmit.co, so this address is never exposed in the browser.
# One-time: the first submission triggers a FormSubmit activation email to this
# address — click the link once to start receiving leads.
QUOTE_TO_EMAIL=omran6143@gmail.com
